<nav class="navbar navbar-expand-lg navbar-light bg-primary bg-gradient px-4 rounded">
  <a class="navbar-brand" href="/"><%= link_to 'DDOS', root_path ,class: "navbar-brand"%></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ms-auto align-items-center">
        <%if user_signed_in? %>
            <li class="nav-item px-1">
            <%= link_to 'SignOut', destroy_user_session_path,method: :delete, class: "navbar-brand" %>
            </li>
        <%else %>
            <li class="nav-item px-1">
                <%= link_to 'Register', new_user_registration_path,  class: "navbar-brand" %>
            </li>
            <li class="nav-item px-1">
            <%= link_to 'SignIn', new_user_session_path,  class: "navbar-brand" %>
            </li>
        <% end %>
        <%if user_signed_in? &&  current_user.user? %>
          <li class="nav-item px-1">
              <%= link_to "Selected Items: #{Cart.where(:user_id => current_user.id).where(:order_id => nil).count}", user_carts_path(:user_id=>current_user.id),  class: "navbar-brand"%>
          </li>
          <li class="nav-item px-1">
              <%= link_to "Ready To Take: #{Order.where(:user_id => current_user.id, :status => "completed").count}", order_completed_user_carts_path(:user_id=>current_user.id),  class: "navbar-brand"%>
          </li>
        <% end %>
        <%if user_signed_in? %>
          <li class="nav-item px-1">
              <%#= link_to 'OrderItem', user_orders_path(:user_id=>current_user.id),  class: "navbar-brand"%>
          </li>
        <% end %>
    </ul>
  </div>
</nav>