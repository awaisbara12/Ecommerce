<div class="container">
    <% @category= Category.find(params[:category_id])%>
    <h1>Add New <%=" "+@category.Type+"  "%>Product</h1>

    <%= render 'form', product: @product %>

    <%= link_to 'Back', admin_products_path(:category_id => params[:category_id]),class: "btn btn-primary" %>
</div>