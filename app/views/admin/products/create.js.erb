<% if @product.errors.any? %>
    alert('Invalid');
<% else %>
    $("#ProductIndex").prepend("<%= escape_javascript(render 'product_row', product: @product)%>");
    $(".close-modal").click();
<%end%>